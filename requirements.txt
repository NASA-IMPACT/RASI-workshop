# Requirements for plot_multifile_var_dashboard_testing_20260326.ipynb
# RASI Multi-File Multi-Variable Plotting with Virtual Zarr

# Data Processing and Analysis
pandas>=1.3.0
numpy>=1.20.0
xarray>=0.18.0
geopandas>=0.9.0
shapely>=1.7.0
pyproj>=3.0.0

# NetCDF and Data Formats
netCDF4>=1.5.6
rioxarray>=0.9.0
zarr>=2.10.0
icechunk>=0.1.0

# Catalog and Client
pystac-client>=0.7.0

# Visualization
matplotlib>=3.3.0
cartopy>=0.18.0
holoviews>=1.14.0
hvplot>=0.7.0
geoviews>=1.9.0
panel>=0.12.0
bokeh>=2.3.0

# For the custom plotutils module
# Note: plotutils is a local module imported from the script directory
# Ensure plotutils.py is present in the same directory as the notebook
